## 代码结构
使用 Go 语言开发，遵循 Go 项目标准目录结构

├── cmd # 组件 main 文件，主要入口
├── config # 配置文件定义、处理、示例等
├── deploy # 各类布署相关文件、目录
├── internal # 核心业务逻辑
└── pkg # 公共组件，业务逻辑无关组件

## 代码风格
1. 遵循 Go 官方代码风格 (gofmt + goimports)
2. 使用 `go vet` 和 `go test` 进行代码检查和测试
3. 变量和函数命名采用驼峰命名法
4. 结构体字段使用驼峰命名法并添加 JSON 标签
5. 包名使用小写单词，尽量简短
6. 配置结构体需要实现 Validate 方法进行验证
7. 对于没有外部依赖的代码，多写单元测试
8. 使用 slog 进行结构化日志

## 错误处理
1. 使用 `deferlog` 搭配 `slog` 包进行错误日志记录
2. 函数返回错误时，应提供清晰的错误信息，使用 `fmt.Errorf` 进行包装
3. 对于关键操作，应实现重试机制

## 构建和部署
1. 使用 Makefile 管理构建过程
2. 在构建时注入版本和日期信息
3. 避免不必要的依赖引入
4. 谨慎引入第三方依赖，对新引入的第三方依赖需给出说明

## 安全规范
1. 输入验证：所有外部输入必须验证
2. 敏感信息：在日志中记录密码和密钥需进行遮蔽处理
3. TLS 配置：使用安全的 TLS 配置
4. 超时设置：所有网络操作设置超时
5. 权限控制：最小权限原则